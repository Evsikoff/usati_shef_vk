{
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:build": "node scripts/verify-build.mjs",
    "test": "npm run build && npm run check:build"
  },
  "dependencies": {
    "@types/ysdk": "^1.2.0"
  }
}
